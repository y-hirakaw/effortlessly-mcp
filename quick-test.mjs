console.log('Testing startup time...'); const start = Date.now(); import('./build/services/lsp/java-lsp.js').then(async ({JavaLSP}) => { const lsp = await JavaLSP.createWithAutoSetup({workspaceRoot: process.cwd() + '/test-java', autoInstall: false}); console.log('Instance created in', (Date.now() - start) + 'ms'); process.exit(0); }).catch(e => { console.error('Error:', e.message); process.exit(1); });
